interface:
  display_name: "Go Docs"
  short_description: "Look up authoritative Go package docs"
  default_prompt: "Use $go-docs to verify a Go package, symbol, signature, or external module API."
policy:
  allow_implicit_invocation: true
