interface:
  display_name: "Go Review"
  short_description: "Review Go code against the bundled ruleset"
  default_prompt: "Use $go-review to audit this Go diff or package against the bundled Go ruleset."
policy:
  allow_implicit_invocation: false
